{% extends 'base.html.twig' %}

{% block title %}Edit Days{% endblock %}

{% block body %}
    <div class="container">
        <h1 id="edit-day">Edit Days</h1>

        {{ include('days_edit/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('days_edit_index') }}" class="btn btn-warning m-1"><i class="fas fa-arrow-alt-circle-left m-2"></i>back to list</a>

        {{ include('days_edit/_delete_form.html.twig') }}
    </div>
{% endblock %}
