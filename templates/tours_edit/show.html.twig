{% extends 'base.html.twig' %}

{% block title %}Tours{% endblock %}

{% block body %}
    <h1>Tours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tour.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tour.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tour.description }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ tour.duration }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ tour.price }}</td>
            </tr>
            <tr>
                <th>Islands</th>
                <td>{{ tour.islands }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ tour.photo }}</td>
            </tr>
            <tr>
                <th>Map</th>
                <td>{{ tour.map }}</td>
            </tr>
            <tr>
                <th>Included</th>
                <td>{{ tour.included }}</td>
            </tr>
            <tr>
                <th>NotIncluded</th>
                <td>{{ tour.notIncluded }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tours_edit_index') }}">back to list</a>

    <a href="{{ path('tours_edit_edit', {'id': tour.id}) }}">edit</a>

    {{ include('tours_edit/_delete_form.html.twig') }}
{% endblock %}
